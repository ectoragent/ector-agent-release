/** Double Ctrl+C window — SSH-like parity across prepend stdin and app handlers. */
export const CTRL_C_DOUBLE_TAP_MS = 1500;
/** Empty Enter twice quickly: interrupt turn or dequeue next message. */
export const DOUBLE_ENTER_MS = 450;