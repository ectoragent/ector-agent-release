# Builtin skills (offline)

Skills **sempre disponíveis** no índice do agente, lidas direto do repositório.

- Não dependem do Hub nem de sync para `~/.ector/skills/`
- Read-only para `skill_manage` (como `external_dirs`)
- Skills criadas pelo agente continuam em `~/.ector/skills/`
